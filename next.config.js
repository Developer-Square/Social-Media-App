/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.pinterest.com', 'unsplash.com', 'source.unsplash.com'],
  },
};

module.exports = nextConfig;
